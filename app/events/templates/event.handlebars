<h2>{{ event.name }}</h2>
<p class="free-text"><strong>When?</strong> {{ event.formattedDate }}</p>
<p class="free-text"><strong>Where?</strong> {{ event.location }}</p>
<p class="free-text">{{{ event.description }}}</p>

<div class="event-controls">
    <a id="to-cal" class="action-button add-calendar-event" href="{{ eventiCal }}" target="_blank">Add to my calendar</a>
</div>
