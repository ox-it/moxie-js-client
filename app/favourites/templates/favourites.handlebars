<h2 class="free-heading">Favourites</h2>
{{#if favourites}}
<ul class="results-list">
    {{#favourites}}
    <a href="#{{path}}">
        <li>
            {{title}}
        </li>
    </a>
    {{/favourites}}
</ul>
{{else}}
    <p class="free-text">You do not have any favourites just yet.</p>
{{/if}}
