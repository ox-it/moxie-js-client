define(["backbone"], function(Backbone) {

    var Item = Backbone.Model.extend({
        // Model Constructor
        initialize: function() {
        }
    });

    // Returns the Model class
    return Item;

});
