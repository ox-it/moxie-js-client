define(["backbone.hal"], function(HAL) {

    var Course = HAL.Model.extend({
        // Model Constructor
        initialize: function() {
        },
    });

    // Returns the Model class
    return Course;

});
