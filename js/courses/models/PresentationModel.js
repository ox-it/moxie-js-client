define(["backbone.hal"], function(HAL) {

    var Presentation = HAL.Model.extend({
        // Model Constructor
        initialize: function() {
        }
    });

    // Returns the Model class
    return Presentation;

});
