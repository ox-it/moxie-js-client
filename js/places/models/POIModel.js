define(["backbone"], function(Backbone) {

    var POI = Backbone.Model.extend({
        // Model Constructor
        initialize: function() {
        }
    });

    // Returns the Model class
    return POI;

});
